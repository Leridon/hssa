package de.thm.mni.hssa

object Formatting {
    
    
    def format(rel: Syntax.Relation): String = {
        type A = Array[String]
        
        rel.body.map[Array[String]] ({
            case Syntax.Assignment(target, )
        })
        
        ???
    }
}
